# Milan Mobile Traffic — Time Series Forecasting

ALU ML Techniques I — Formative Assignment | **Nick-Lemy Kayiranga**

- **Report**: `MLTechniques1_Nick-Lemy_Formative1.pdf`
- **Video demo**: [Add link here]

## Project Structure

```
formative1_ml_techniques1/
├── dataset/                      # Raw zip files (not committed to git)
│   ├── dataverse_files.zip
│   ├── dataverse_files(1).zip
│   └── ...
├── data/processed/               # Generated by notebook 01
│   ├── milan_traffic_pivot.parquet
│   └── target_areas.json
├── notebooks/
│   ├── 01_data_handling.ipynb    # Task 1: load, optimise, save parquet
│   ├── 02_eda.ipynb              # Task 2: EDA & characterisation
│   └── 03_forecasting.ipynb     # Task 3: SARIMA, LSTM, TCN
├── outputs/
│   ├── figures/                  # All plots (PNG)
│   ├── tables/                   # Performance & timing CSVs
│   └── models/                   # Saved PyTorch weights
├── run_tuning.py                 # Standalone hyperparameter tuning script
├── MLTechniques1_Nick-Lemy_Formative1.pdf  # Submission report
├── requirements.txt
└── README.md
```

## How to Run

### Prerequisites

- Python 3.10+
- ~10 GB free RAM recommended
- The 7 zip files in `dataset/` (not redistributed; download from Harvard Dataverse)

### Setup (Linux / macOS)

```bash
python3 -m venv .venv
source .venv/bin/activate
pip install -r requirements.txt
python -m ipykernel install --user --name milan-traffic --display-name "Python 3 (.venv)"
jupyter lab
```

### Setup (Windows)

```powershell
python -m venv .venv
.venv\Scripts\activate
pip install -r requirements.txt
python -m ipykernel install --user --name milan-traffic --display-name "Python 3 (.venv)"
jupyter lab
```

### Run Order

Open notebooks in order and select the **"Python 3 (.venv)"** kernel:

1. `01_data_handling.ipynb` — reads all zips, produces `data/processed/milan_traffic_pivot.parquet`
2. `02_eda.ipynb` — loads parquet, produces all EDA figures
3. `03_forecasting.ipynb` — trains & evaluates SARIMA, LSTM, TCN models

**Note**: notebook 01 takes ~15–30 minutes depending on hardware. Notebooks 02 and 03 load the pre-built parquet and are much faster.

## Dataset

- Telecom Italia Mobile (TIM) Milan dataset
- [Harvard Dataverse — Telecom activity](https://dataverse.harvard.edu/dataset.xhtml?persistentId=doi:10.7910/DVN/EGZHFV)
- [Harvard Dataverse — Grid](https://dataverse.harvard.edu/dataset.xhtml?persistentId=doi:10.7910/DVN/QJWLFU)
